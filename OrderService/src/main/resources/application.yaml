spring:
  application:
    name: OrderService

  datasource:
    url: jdbc:h2:mem:microservices
    driverClassName: org.h2.Driver
    username: sa
    password:

server:
  port: 8080

user-service:
  base-url: http://localhost:8090
